# Password generator

A project I made during a computer science lesson for fun, cause the python work we were doing was boring.

## Purpose

Generates passwords (llok at the title)

## Usage

This uses uv, so install that and run `uv run main.py`.
